import { View, Text } from "react-native";

const Config = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 18, fontWeight: "bold", color: "#383838" }}>
        Esta es la de Config
      </Text>
    </View>
  );
};

export default Config;
